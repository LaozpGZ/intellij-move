• 已继续推进，GAP-SUI-05 (equality auto-referencing) 这轮我已经落地完成了。

  - 核心推断已完成：`==/!=` 在 `T` vs `&T`、`&T` vs `T` 场景下支持自动借用，并补了双向兼容检查，避免仅单向组合导致的误报。
    代码在 `src/main/kotlin/org/sui/lang/core/types/infer/TypeInferenceWalker.kt`（`inferEqualityBinaryExprTy` 及新增 equality helper）。
  - 回归测试已补齐：types 与 inspection 都新增了正反向样例。
    见 `src/test/kotlin/org/sui/lang/types/ExpressionTypesTest.kt`、`src/test/kotlin/org/sui/ide/inspections/MvTypeCheckInspectionTest.kt`。
  - 任务文档已勾选完成：`docs/github-issues/sui-move-2024/05-equality-auto-referencing.md`。

  验证结果（全部通过）：

  - `./gradlew test --tests "org.sui.lang.types.ExpressionTypesTest" --tests "org.sui.ide.inspections.MvTypeCheckInspectionTest" --no-daemon`
  - `./gradlew test --no-daemon`

  下一步可以直接继续：

  1. 开始 GAP-SUI-06（nested `use` 深层组合 + std 默认导入行为回归锁定）。
  2. 或先推进 GAP-SUI-08（`verifyPlugin` deprecated/internal 热点治理与门禁收紧）。

• 已继续推进，GAP-SUI-04 (match guard-aware diagnostics) 这轮我已经落地完成了。

  - 核心语义改造已完成：guard 分支不再被当作“完整覆盖”，不可达与穷尽性按 guard-aware 规则重新判定。
    代码在 src/main/kotlin/org/sui/ide/annotator/MvErrorAnnotator.kt:205、src/main/kotlin/org/sui/ide/annotator/MvErrorAnnotator.kt:250、src/main/kotlin/org/sui/ide/annotator/
    MvErrorAnnotator.kt:301。
  - 回归测试已补齐 4 个 guard 边界场景。
    见 src/test/kotlin/org/sui/ide/annotator/errors/MatchArmDuplicateErrorTest.kt:39、src/test/kotlin/org/sui/ide/annotator/errors/MatchArmDuplicateErrorTest.kt:69、src/test/
    kotlin/org/sui/ide/annotator/errors/MatchArmDuplicateErrorTest.kt:99、src/test/kotlin/org/sui/ide/annotator/errors/MatchArmDuplicateErrorTest.kt:114。
  - 任务文档已勾选完成：docs/github-issues/sui-move-2024/04-match-guard-aware-diagnostics.md:11。
  - 新增当日进展记录：docs/2026-2-22.md:1。

  验证结果（全部通过）：

  - ./gradlew test --tests "org.sui.ide.annotator.errors.MatchArmDuplicateErrorTest" --no-daemon
  - ./gradlew test --tests "org.sui.ide.annotator.errors.*" --no-daemon
  - ./gradlew test --no-daemon
  - ./gradlew verifyPlugin --no-daemon（Compatible IU-253.31033.19）
  - ORG_GRADLE_PROJECT_shortPlatformVersion=261 ./gradlew verifyPlugin --no-daemon（Compatible IU-261.21525.39）

  下一步可以直接继续：

  1. 开始 GAP-SUI-05（equality automatic referencing，types + inspection + tests）。
  2. 先把 verifyPlugin 的 deprecated/internal 热点清单化（对应 GAP-SUI-08 前置治理）。
