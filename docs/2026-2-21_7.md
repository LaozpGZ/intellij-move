 继续推进已完成，GAP-SUI-01 (positional fields) 这轮已经收口到可发布门禁级别。

  1. 修复了 numeric struct pattern 的类型推断主缺口：src/main/kotlin/org/sui/lang/core/types/infer/Patterns.kt:67，src/main/kotlin/org/sui/lang/core/types/infer/
     Patterns.kt:71。
  2. 统一字段抽象入口为包含 tuple fields：src/main/kotlin/org/sui/lang/core/psi/ext/MvFieldsOwner.kt:31。
  3. 新增回归测试覆盖 numeric pattern 绑定类型与检查错误：src/test/kotlin/org/sui/lang/types/ExpressionTypesTest.kt:220，src/test/kotlin/org/sui/lang/types/
     ExpressionTypesTest.kt:233，src/test/kotlin/org/sui/ide/inspections/MvTypeCheckInspectionTest.kt:1239。
  4. 任务文档已同步勾选完成并写入进展：docs/github-issues/sui-move-2024/01-positional-fields.md:11，docs/github-issues/sui-move-2024/01-positional-fields.md:20，docs/2026-2-
     21_6.md:1。

  验证结果：

  1. ./gradlew test --tests "org.sui.lang.types.ExpressionTypesTest" --tests "org.sui.ide.inspections.MvTypeCheckInspectionTest" --tests
     "org.sui.lang.resolve.ResolveStructFieldsTest" --no-daemon 通过。
  2. ./gradlew test --no-daemon 全量通过。
  3. ./gradlew verifyPlugin --no-daemon 通过，Compatible (IU-253.31033.19)。
  4. ORG_GRADLE_PROJECT_shortPlatformVersion=261 ./gradlew verifyPlugin --no-daemon 通过，Compatible (IU-261.21525.39)。

  注意：

  1. verifyPlugin 过程中有既有噪音日志（如 documented API changes 抓取 SSL 报错、caffeine invalid state 日志），但任务最终均 BUILD SUCCESSFUL，兼容性结论有效。
  2. 工作区里还保留你前一轮 revised paths 的改动和未跟踪文件，我没有回滚。

  下一步我可以直接继续做：

  1. GAP-SUI-04 match guard-aware（annotator/type 语义补齐 + 回归测试）。
  2. 或先专门清理 verifyPlugin 输出里的 deprecated/internal 热点。