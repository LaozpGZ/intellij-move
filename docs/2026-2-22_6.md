• 本次 GAP-SUI-08 已完成，按你的要求仅交付这一项结果。

  已完成内容：

  1. 固化 verifier IDE 坐标与 installer 方式

  - 修改：build.gradle.kts、gradle-253.properties、gradle-261.properties
  - 新增可配置项：verifierUseInstaller、verifierAllowInternalApiUsages
  - 默认收紧为：INTERNAL_API_USAGES 不再豁免（verifierAllowInternalApiUsages=false）

  2. 清零 INTERNAL_API_USAGES

  - 修改：src/main/kotlin/org/sui/cli/settings/MvProjectSettingsService.kt
    移除对 BaseState.__getProperties() / StoredProperty.isEqualToDefault() 的依赖
  - 修改：src/main/kotlin/org/sui/cli/sentryReporter/SentryErrorReporter.kt
    移除 DiagnosticBundle internal API 依赖

  3. CI/PR 输出 verifier 摘要与报告

  - 新增：.github/scripts/plugin-verifier-summary.sh
  - 修改：.github/workflows/check.yml
      - verifyPlugin 后输出摘要到日志与 GITHUB_STEP_SUMMARY
      - 上传 build/reports/pluginVerifier/ artifact
      - 增加 internal usage 预算门禁：PLUGIN_VERIFIER_MAX_INTERNAL_API_USAGES=0

  4. 治理文档与任务状态

  - 新增：docs/verifyplugin-hardening.md（含 INTERNAL_API_USAGES 清单、替代方案、owner、分阶段收紧计划）
  - 勾选完成：docs/github-issues/sui-move-2024/08-verifyplugin-hardening.md
  - 续写记录：docs/2026-2-22_4.md

  验证结果（本地）：

  - ./gradlew verifyPlugin --no-daemon（253）通过
      - verdict: Compatible
      - internal=0，scheduled=12，deprecated=28，experimental=119
  - ORG_GRADLE_PROJECT_shortPlatformVersion=261 ./gradlew verifyPlugin --no-daemon（261）通过
      - verdict: Compatible
      - internal=0，scheduled=12，deprecated=38，experimental=119

  另外，按你指示，本次未处理 docs/2026-2-22_5.md。