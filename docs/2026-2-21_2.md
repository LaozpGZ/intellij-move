  本次已完成

  1. 修复 261 下 Grammar 生成链阻塞：build.gradle.kts:65 把
     org.jetbrains.grammarkit 升级到 2023.3.0.2。
  2. 回收 CI 临时绕过参数：/.github/workflows/
     check.yml:110、/.github/workflows/check.yml:130 已去掉 -x
     generateLexer -x generateParser。
  3. 同步任务目标风险状态：docs/2026-1_todo.md:72 从“失败后临时跳
     过”更新为“已修复并持续监控”。
  4. 重写进展记录：docs/2026-2-21.md:1（现在是正式里程碑记录+下一步
     执行计划）。
  5. 更新 AGENTS 当前目标状态：AGENTS.md:7 增加“261 已恢复全链路预验
     证”。

  本地验证结果

  1. ORG_GRADLE_PROJECT_shortPlatformVersion=261 ./gradlew
     generateParser --no-daemon --stacktrace：通过。
  2. ORG_GRADLE_PROJECT_shortPlatformVersion=261 ./gradlew
     verifyPluginProjectConfiguration verifyPlugin
     -Pkotlin.incremental=false --no-daemon：通过，Verifier
     Compatible (IU-261.21525.39)。
  3. ORG_GRADLE_PROJECT_shortPlatformVersion=253 ./gradlew test
     -Pkotlin.incremental=false --no-daemon：通过。
  4. ORG_GRADLE_PROJECT_shortPlatformVersion=253 ./gradlew
     verifyPluginProjectConfiguration verifyPlugin
     -Pkotlin.incremental=false --no-daemon：通过，Verifier
     Compatible (IU-253.31033.19)。

  详细规划（下一步）

  1. CI 观察窗（2-3 天）
     执行 tests(253) + eap-preverify(261)，目标是连续 3 次全绿；若波
     动，先定位是否由 EAP 版本漂移引起。
  2. M3 功能攻坚（按 P1 顺序）
     先做 positional fields，再做 break value，再做 revised paths；
     每项都要求 parser/resolve/types(or annotator)/completion 对应测
     试一起落地。
  3. 发布收口（M4）
     补齐回归清单、更新用户文档与迁移说明、完成 buildPlugin + 手工样
     例验收后进入发布。

  如果你同意，我下一步就直接从 P1 的 positional fields 开始拆任务并
  落代码。
