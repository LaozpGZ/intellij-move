完成内容

1. 修复 nested use 的叶子展开与解析链路，支持多层 use group 递归展开，并补齐地址级 nested use（`std::{...}` / `0x1::{...}`）模块解析。
   - `src/main/kotlin/org/sui/lang/core/resolve2/util/PathUtil.kt`
   - `src/main/kotlin/org/sui/lang/core/resolve2/LexicalDeclarations2.kt`
   - `src/main/kotlin/org/sui/lang/core/resolve2/PathKind.kt`
   - `src/main/kotlin/org/sui/lang/core/resolve2/ref/MvPath2ReferenceImpl.kt`
2. 新增 resolve 回归：多层 nested use（含 `Self` + alias 混合）、value address nested use。
   - `src/test/kotlin/org/sui/lang/resolve/ResolveModulesTest.kt`
3. 新增 optimize imports 回归：nested use 场景下仅移除未使用叶子导入，保留仍被引用的导入，避免语义破坏。
   - `src/test/kotlin/org/sui/ide/refactoring/optimizeImports/OptimizeImportsTest.kt`
4. 新增 completion 回归（含 project 级）：nested use 场景下模块/项补全可用并稳定。
   - `src/test/kotlin/org/sui/lang/completion/names/ModulesCompletionTest.kt`
   - `src/test/kotlin/org/sui/lang/completion/names/project/ModulesCompletionProjectTest.kt`
5. 勾选 GAP-SUI-06 任务文档。
   - `docs/github-issues/sui-move-2024/06-nested-use-and-std-default-use.md`

验证结果

1. `./gradlew test --tests "org.sui.lang.resolve.ResolveModulesTest" --tests "org.sui.lang.completion.names.ModulesCompletionTest" --tests "org.sui.lang.completion.names.project.ModulesCompletionProjectTest" --tests "org.sui.ide.refactoring.optimizeImports.OptimizeImportsTest" --no-daemon`
2. `./gradlew test --no-daemon`
3. 均通过（`BUILD SUCCESSFUL`）。
